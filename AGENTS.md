# Spec

 * in `~/downloads/pdfcpu_0.11.1_Js_wasm`, the latest wasm build of pdfcpu (https://github.com/pdfcpu/) is located
 * create a webpage that takes up to 10 pdf files as input, and merges them with pdfcpu
 * allow user to download merged pdf
 * no server-side components, this should be happening purely in the browser
 * add a section at the bottom that explains how all of this browser-only and the pdf will not be sent anywhere. also include a link to pdfcpu and to the webassembly wikipedia
 * this page will later be available as https://ramseyer.it/joinpdf and be called JoinPDF. add some cool branding in the HTML and the cover pag
 * add an option to insert a blank page inbetween
 * also add an option for a cover page, that will list the documents that follow
